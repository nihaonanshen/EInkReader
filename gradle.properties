org.gradle.daemon=true
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true

# Native debugging should be disabled in production
android.nativeDebugging=false
android.suppressUnsupportedCompileSdk=35
android.javaCompile.suppressSourceTargetDeprecationWarning=true

# Signing credentials (moved from build.gradle for security)
# SIGNING_STORE_PASSWORD=<set via environment variable or local.properties>
# SIGNING_KEY_PASSWORD=<set via environment variable or local.properties>
# SIGNING_STORE_FILE=einkreader.jks
